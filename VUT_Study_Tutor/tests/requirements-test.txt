# Optional regression-test environment only. Do NOT install into Desktop's Python.
# Versions used on the Linux build host; not new runtime dependencies.
fastapi==0.128.2
starlette==0.50.0
httpx==0.28.1
python-multipart==0.0.29
pydantic==2.13.4
